<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title><%= content_for(:title) || "e_ren SPACE - Campus Events" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="e_ren SPACE">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Google Fonts for Cyberpunk style %>
    <link href="https://fonts.googleapis.com/css2?family=Archivo+Black&family=JetBrains+Mono:wght@400;700;800&display=swap" rel="stylesheet">

    <%# Tailwind CSS CDN with cyberpunk theme %>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              'cyber': {
                'bg': '#121214',
                'card': '#1e1e24',
                'crimson': '#ff1f4b',
                'lime': '#ccff00',
                'grey': '#a0a0a0',
              }
            },
            fontFamily: {
              'mono': ['JetBrains Mono', 'monospace'],
              'display': ['Archivo Black', 'sans-serif']
            },
            backgroundImage: {
              'cyber-grid': 'linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px), linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px)'
            },
            backgroundSize: {
              'grid': '40px 40px'
            }
          }
        }
      }
    </script>

    <style>
      /* Only custom styles Tailwind can't handle */
      body {
        background-image: linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px),
                          linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
        background-size: 40px 40px;
      }

      .skew-card { transform: skewX(-10deg); }
      .skew-card > * { transform: skewX(10deg); }

      .skew-btn { transform: skewX(-10deg); }
      .skew-btn:hover { transform: skewX(-10deg) translate(-2px, -2px); }
      .skew-btn:active { transform: skewX(-10deg) translate(2px, 2px); }

      .text-shadow-cyber { text-shadow: 2px 2px 0px #ff1f4b; }
      .text-shadow-cyber:hover { text-shadow: 3px 3px 0px #ff1f4b, -1px -1px 0px #ccff00; }

      .glow-lime { box-shadow: 0 0 10px #ccff00; }
    </style>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-cyber-bg text-white font-mono overflow-x-hidden">
    <%
      show_alert = alert.present? && alert != "You need to sign in or sign up before continuing."
    %>
    <% if notice.present? || show_alert %>
      <div class="fixed top-4 right-4 z-50 space-y-2" data-turbo-temporary>
        <% if notice.present? %>
          <div class="bg-cyber-card border-2 border-cyber-lime text-cyber-lime px-6 py-4 rounded max-w-md animate-pulse">
            <div class="flex items-start">
              <span class="mr-3">✓</span>
              <div>
                <p class="font-bold uppercase text-sm">Success</p>
                <p class="text-sm opacity-80"><%= notice %></p>
              </div>
            </div>
          </div>
        <% end %>
        <% if show_alert %>
          <div class="bg-cyber-card border-2 border-cyber-crimson text-cyber-crimson px-6 py-4 rounded max-w-md">
            <div class="flex items-start">
              <span class="mr-3">✕</span>
              <div>
                <p class="font-bold uppercase text-sm">Error</p>
                <p class="text-sm opacity-80"><%= alert %></p>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
